# Frontend environment variables — copy to .env and fill in actual values.
# VITE_ prefix is required for Vite to expose variables to the browser bundle.
# Only the public key_id belongs here — the secret stays in functions/.env.
VITE_RAZORPAY_KEY_ID=rzp_test_...
