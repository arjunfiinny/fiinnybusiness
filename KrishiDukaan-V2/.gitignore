# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
# env backups / variants — may contain LIVE secrets, never commit
.env*.local.*
.env*.bak*
*.bak-*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Windows metadata
*:Zone.Identifier

# Firebase CLI debug logs
firebase-debug.log
firebase-debug.*.log

# Engineering wiki — deliberately NOT in this repository.
# It documents live production internals, including an audit page with working
# exploit chains, and the gate app hardcodes the wiki password. This repo is a
# PUBLIC fork, so the wiki is kept local and published only to the
# password-gated site (krishidukan-wiki.web.app).
wiki/
wiki-site/
firebase.wiki.json

# Firebase CLI debug log (repo root)
firebase-debug.log
firebase-debug.*.log
