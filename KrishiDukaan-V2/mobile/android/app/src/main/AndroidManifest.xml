<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!-- Push notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="KrishiDukaan"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link for invite codes and products -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="krishidukan.com" />
                <data android:scheme="https" android:host="karan-arjun-uat.web.app" />
            </intent-filter>
        </activity>

        <!-- Google Maps SDK — same key as GOOGLE_MAPS_API_KEY / AppConfig.googleMapsApiKey,
             restricted to this app's package + SHA-1 in Google Cloud Console. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDh_Y67TDJc2KLLJ8Wcc2JvEeHzmfVL778"/>

        <!-- FCM default notification icon and color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher"/>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color"/>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="krishidukaan_main"/>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Lets Razorpay's checkout detect installed UPI apps (PhonePe,
             Google Pay, Paytm, BHIM, etc.) so it can offer the fast "tap the
             app icon" UPI intent flow instead of forcing every customer onto
             manual UPI-ID entry.
             Without this, Android 11+ package-visibility rules hide every
             other app's package from this app's PackageManager, so
             Razorpay's own installed-app check finds nothing and silently
             falls back to the ID-entry (collect) flow — which is also the
             slower flow that's more likely to trip the checkout SDK's own
             completion timeout (see order-status/route.ts for the other half
             of that fix). The generic upi://pay intent below is what every
             UPI app registers, so this alone covers all of them without
             listing packages one by one. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="upi" android:host="pay"/>
        </intent>
    </queries>
</manifest>
